/*
 *
 */

#include "dsensor_driver.h"

// read the current state
void get_distance(n64_state_t* state)
{
    volatile n64_state_t* address = (volatile n64_state_t*)N64_ADDR;
    *state = *address;
}
